# 理杏仁迁移实施 — 独立审核报告

> **审核日期**：2026-06-27  
> **审核范围**：feat/lixinger-p1 ~ p6 全部 6 阶段，23 文件，+4,845 行  
> **审核人**：独立审核 Agent（未参与编码）  
> **审核依据**：`docs/plan-lixinger-migration.md` 全部验收标准  
> **最终判决**：✅ 准出

---

## 审核方法论

逐项对照规划文档验收标准，分"代码静态审查 + 实机运行验证"两轮。

- **静态审查**：逐文件检查架构设计、回退链、安全策略、编码规范
- **实机验证**：对关键验收项（EV/NBV、数据偏差、测试套件）实际运行理杏仁 API 取值计算

---

## 阶段 1：核心基础设施

| # | 验收项 | 判定 | 证据 |
|---|--------|------|------|
| P1.1 | `tools/lxr_client.py` — HTTP POST 客户端，处理 token/限流/重试/gzip | ✅ | 301 行，完整异常分类（Auth/RateLimit/Validation/Server/Timeout），指数退避重试 |
| P1.2 | `tools/lxr_cache.py` — 缓存路径 `tempfile.gettempdir()` | ✅ | 行35，跨平台，非硬编码 |
| P1.3 | `lxr_config.json` 被 `.gitignore` 排除 | ✅ | `git ls-files` 确认不在索引中 |
| P1.3 | `lxr_config.example.json` 已提交 | ✅ | token 字段为空字符串 |
| P1.4 | 三阶回退链 `理杏仁 → 妙想 → 免费源` | ✅ | `_run_chain()` 异常跳过，全部失败返回 `_source: "none"` |
| P1.4 | 失败标注 `_source` 字段 | ✅ | 所有返回 dict 含 _source |
| P1.5 | `.gitignore` 含缓存目录 | ✅ | `lxr_cache/` + `__pycache__/` 均排除 |

## 阶段 2：A 股数据替换

| # | 验收项 | 判定 | 证据 |
|---|--------|------|------|
| P2.1 | `get_financials()` 自动判定 5 种财报类型 | ✅ | `detect_fs_type()` 通过 `{market}/company` 获取 `fsTableType` |
| P2.2 | `get_valuation()` 含历史分位点 | ✅ | 估值 + 12 个分位点维度 |
| P2.3 | `ashare_data.py --source lixinger` | ✅ | 默认行为不变（`--source default`） |
| P2.4 | `financial_rigor.py --source lixinger` | ✅ | 自动取股价/股本/市值/EPS/BVPS 验算 |
| P2.5 | `financial-data.md` 更新 | ✅ | 新增三级数据源章节 |
| P2.6 | 600519/601336 偏差验证 | ✅ **实测** | 见下方实机验证 |

## 阶段 3：港股 + 新增能力

| # | 验收项 | 判定 | 证据 |
|---|--------|------|------|
| P3.1 | 港股财务数据 | ✅ | 市场自动路由，代码补零至 5 位 |
| P3.2 | 港股 K 线 | ✅ | `hk/company/candlestick`，前复权 |
| P3.3 | `stock_screener.py --source lixinger` | ✅ | 美股自动回退 Yahoo |
| P3.4 | 股东分析（A 股 + 港股） | ✅ | 前十大/股东人数/基金持股全覆盖 |
| P3.5 | 估值分位点全矩阵 | ✅ | 4 指标 × 6 维度 × 8 统计，分批请求合并 |

## 阶段 4：保险/金融专属

| # | 验收项 | 判定 | 证据 |
|---|--------|------|------|
| P4.1 | EV/NBV 验证 | ✅ **实测** | 见下方实机验证，EV/NBV 均在 API 返回中 |
| P4.2 | 保险行业深度 | ✅ | ~128 有效指标，含 EV/NBV/偿付能力/营运利润 |
| P4.3 | 银行/证券深度 | ✅ | 银行 19 项（资本充足率/净息差/不良/拨备），证券 14 项（经纪/投行/资管/自营） |
| P4.4 | 营收构成 | ✅ | 分产品/分地区收入 + 占比 + 毛利率 |
| P4.5 | 公司治理 | ✅ | 高管增减持 + 大股东增减持（A 股），董事权益变动（港股） |

## 阶段 5-6：宏观/指数/文档

| # | 验收项 | 判定 | 证据 |
|---|--------|------|------|
| P5.1 | 宏观：国债收益率/利率 | ✅ | `get_bond_yield_10y()` 利差分析基准 |
| P5.2 | 指数估值 | ✅ | 沪深 300/恒生 PE/PB/PS/股息率 + 10 年分位 |
| P5.3 | 申万行业对比 | ✅ | `compare_industry_valuation()` 一键输出对比表 |
| P6.1 | CLAUDE.md 数据源更新 | ✅ | 三级降级链说明完整 |
| P6.2 | 文档 + 帮助 | ✅ | CLI 示例完整 + `docs/lixinger-data-guide.md` |
| P6.3 | codex/ 目录同步 | ✅ | 4 个 lxr_* 文件已同步 |

## 通用质量

| # | 验收项 | 判定 | 证据 |
|---|--------|------|------|
| G1 | PEP 8 + 类型注解 | ✅ | 语法检查通过，类型注解完整 |
| G2 | 无硬编码路径 | ✅ | `tempfile.gettempdir()` + `os.path.dirname(__file__)` |
| G3 | Token 安全 | ✅ | `.gitignore` 排除；缓存指纹剔除 token 键 |
| G4 | 无命令注入 | ✅ | subprocess 使用 list 参数（非 shell=True） |
| G5 | Git 工作流 | ✅ | 6 个独立 feature 分支，逐阶段 merge |
| G6 | 测试覆盖 | ✅ | 38 项全通过（0.70s），覆盖缓存/降级链/路由/剪枝/矩阵/行业深度 |

---

## 实机验证

### 验证 1：EV/NBV 真实性（601336 新华保险）

```
python tools/lxr_data.py industry-deep 601336 --years 3
```

结果：

| 字段 | 2025 | 2024 | 2023 |
|------|------|------|------|
| EV（内含价值） | 2,878.40亿 | 2,584.48亿 | 2,505.10亿 |
| NBV（新业务价值） | 98.42亿 | 62.53亿 | 30.24亿 |
| 核心偿付能力充足率 | 135.11% | 124.07% | 157.01% |
| 综合偿付能力充足率 | 210.47% | 217.55% | 278.43% |

- 有效指标数：**128**（>100 准出标准）
- **结论：EV 和 NBV 均在理杏仁 insurance 报表中，数据完整可追溯。**

### 验证 2：数据偏差（600519 贵州茅台）

| 年度 | 归母净利润（东财默认源） | 归母净利润（理杏仁） | 偏差 |
|------|------------------------|-------------------|------|
| 2025 | 823.20亿 | 823.20亿 | 0.00% |
| 2024 | 862.28亿 | 862.28亿 | 0.00% |
| 2023 | 747.34亿 | 747.34亿 | 0.00% |
| 2022 | 627.17亿 | 627.16亿 | **0.0016%** |
| 2021 | 524.60亿 | 524.60亿 | 0.00% |

市值验算（理杏仁自动）：股价 1271.1 × 12.56亿股 = 1.60万亿 vs 报告 1.59万亿 → 偏差 0.49%

- **结论：最大偏差 0.0016%，远低于 0.5% 准出阈值。市值验算偏差 0.49%，同样通过。**

### 验证 3：测试套件

```
38 passed in 0.70s
```

覆盖：缓存 key 去 token / TTL 过期 / 禁用缓存 / token 解析（env/显式/缺失）/ 429 重试成功 / 持续 429 抛限流 / 400 校验错误解析 / 降级链（首项成功/跳过异常/全部失败）/ 市场探测与代码归一 / 无效指标自动剪枝 / 端点路由 / 分位点矩阵 / 行业深度摘要 / 宏观利率 latest / 行业估值无 cp

- **结论：全部 38 项通过，零失败。**

---

## 总体评价

实施质量良好，实现了规划文档的全部核心目标：

**亮点**：
- 三层降级链设计严谨，`_source` 字段全程追溯数据来源
- 自动指标剪枝机制实用——提交 128 个候选指标，API 不支持的自动剔除重试，无需人工维护兼容性列表
- 异常分类精细（Auth/RateLimit/Validation/Server/Timeout），每种有独立处理策略
- 缓存指纹剔除 token 键，避免 token 泄露到磁盘文件名
- 测试覆盖全面（38 项），纯 mock 不依赖网络，CI 友好

**改进建议（非阻塞）**：
- `lxr_data.py` 1,406 行偏长（项目规范建议 ≤800 行），后续可按模块拆分（宏观/行业/治理各自独立）
- `deep_summary` 中 `opatshopc` 和 `pi` 为 null 时未区分"API 不支持"与"数据缺漏"，建议标注原因

---

## 最终判决：✅ 准出

三项准出条件（EV/NBV 真实性、数据偏差、测试套件）全部通过实机验证。无阻塞项。代码可以合入 main 分支并推送远程。

---

*审核报告由独立审核 Agent 生成，未经编程 Agent 干预。*
